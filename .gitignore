# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# IP geolocation databases (DB-IP Lite).
# The ASN DB (~9 MB, for ISP) IS bundled/deployed. The City DB (~124 MB) exceeds
# Vercel's 100 MB per-file limit, so it's kept local-only (dev fallback); in
# production city/region/country come from the platform's edge geo headers.
/data/dbip-city-lite.mmdb

# local editor/agent settings
/.claude/

.vercel

# internal docs (e.g. competitor analysis, audits) — not for the public repo
*.pdf
*audit*.md
